# 参与贡献

感谢你对 Akino 的关注！无论是修复 Bug、新增功能、改进文档、翻译界面，还是只是报告问题，每一份贡献都值得尊重。请先花几分钟阅读本指南，它能让你的贡献更顺畅，也让维护者更高效。

> 本项目依据以下通用规范维护，请在提交任何贡献前完整阅读本文档。

---

## 目录

- [参与贡献](#参与贡献)
  - [目录](#目录)
  - [贡献方式](#贡献方式)
  - [环境要求](#环境要求)
  - [本地开发](#本地开发)
  - [代码规范](#代码规范)
  - [提交信息规范](#提交信息规范)
  - [AI 辅助贡献政策](#ai-辅助贡献政策)
  - [提交 Pull Request](#提交-pull-request)
  - [代码审查](#代码审查)
  - [报告问题](#报告问题)
  - [行为准则](#行为准则)
  - [许可证](#许可证)

---

## 贡献方式

你可以通过以下任意一种方式参与：

- **报告 Bug**：发现崩溃、错误行为或构建失败时，请先搜索 [Issues](https://github.com/adproqwq/akino/issues) 确认是否已被报告；若没有，请新建 Issue 并说明复现步骤、运行环境（操作系统、Node.js 版本）和期望行为。
- **提出新功能**：在 Issue 中描述需求场景和预期效果，标注为 `Feature`，供社区讨论。
- **提交代码**：修复 Bug、实现功能、重构代码，通过 Pull Request 提交。
- **改进文档**：修正 README、注释、拼写错误，或补充使用说明。
- **翻译**：维护 `src/renderer/src/locales/` 下的语言包，或翻译本文档与 README。
- **参与讨论**：回答 Issue 中的问题，参与功能设计讨论，评审他人的 Pull Request。

## 环境要求

- [Node.js](https://nodejs.org/) 22 或更高版本（建议 LTS）。
- [pnpm](https://pnpm.io/) 11 或更高版本（项目通过 `packageManager` 字段锁定为 pnpm 11.24.0）。
- Git 客户端。
- 打包验证：构建 macOS 安装包需要在 macOS 上进行；Windows、Linux 安装包需要对应平台或 Electron Builder 支持的交叉构建环境。

## 本地开发

```bash
# 1. 安装依赖
pnpm install          # 或 pnpm ci（严格按 lockfile 安装）

# 2. 启动开发模式（渲染进程支持热更新）
pnpm dev

# 3. 构建产物
pnpm build            # 合并 main / preload / renderer 构建

# 4. 打包安装包（仅本机验证用，发布由 CI / Release 流程负责）
pnpm build:win        # Windows NSIS 安装包
pnpm build:mac        # macOS DMG
pnpm build:linux      # Linux AppImage / deb / snap
pnpm build:unpack     # 未打包目录
```

> 注意：所有打包脚本均带有 `--publish never`，只在本机构建产物，不会触发任何发布操作。

## 代码规范

- **缩进与格式**：遵循根目录 `.editorconfig`（2 空格缩进、UTF-8、LF 换行、末尾空行）。
- **语言**：TypeScript，请尽量提供类型标注，避免使用 `any`。
- **主进程**（`src/main/`）：维护 `AkinatorClient` 生命周期与 IPC 接口，不要在前端直接调用 Node API。
- **预加载脚本**（`src/preload/`）：通过 `contextBridge` 安全暴露 API，遵循最小暴露原则。
- **渲染进程**（`src/renderer/`）：Vue 3 `<script setup lang="ts">` 组合式 API；改动 UI 后应自测三种主题（亮/暗/跟随系统）与中英双语。
- **命名**：文件、变量、函数使用有意义的英文命名；组件用 PascalCase，普通文件用 camelCase。
- **提交前自检**：
  - 代码可被 `pnpm build` 正常构建；
  - 避免引入新的运行时依赖，如确有必要，请在 PR 描述中说明理由。

## 提交信息规范

提交信息使用 [Conventional Commits](https://www.conventionalcommits.org/) 约定：

```
<type>(<scope>): <description>
```

常用类型：

| 类型 | 用途 |
| --- | --- |
| `feat` | 新功能 |
| `fix` | 修复 Bug |
| `docs` | 仅文档变更 |
| `style` | 不影响逻辑的格式调整 |
| `refactor` | 重构（不改变行为） |
| `perf` | 性能优化 |
| `test` | 添加或修改测试 |
| `chore` | 构建、依赖、工具链等杂项 |
| `i18n` | 语言包 / 翻译改动 |

示例：

```
fix(game): 防止点击回答后重复触发两次请求
feat(settings): 支持配置 HTTP 代理
docs: 补充开发文档
```

## AI 辅助贡献政策

本项目**允许使用 AI 辅助工具**（如代码生成、自动补全、翻译、文档撰写、测试编写等）完成贡献，但**所有 AI 生成或 AI 辅助修改的内容，在提交前必须经过人工审计**。

具体要求如下：

1. **人工签名与负责**：AI 只是工具，贡献者本人是提交内容的最终责任人。提交 PR 即表示你已逐行审阅并理解所有改动，能够就其中的任何部分作出解释。
2. **人工审计清单**（每一条都必须落实）：
   - **正确性**：改动符合 Issue / 需求描述，逻辑自洽，边界情况已考虑；
   - **安全性**：未引入凭据、令牌、内部路径或敏感数据；涉及外部输入与 IPC 的改动需重点检查；
   - **合规性**：新增依赖与代码片段符合开源许可证要求，不引入有争议的许可证；
   - **一致性**：遵循上方"代码规范"，与既有代码风格、架构保持一致；
   - **可维护性**：命名清晰、无冗余代码、注释说明"为什么"而非"是什么"。
3. **必要验证**：AI 生成的改动必须经过实际运行验证（至少 `pnpm build` 通过；涉及行为的改动请在本地运行 `pnpm dev` 自测），不能省去验证环节直接提交。
4. **披露（推荐）**：在 PR 描述中说明"本改动使用了 AI 辅助工具（如 …）"，有助于维护者针对性审查；如未披露，维护者会按常规流程审查。
5. **禁止事项**：
   - 禁止将任何密钥、令牌提供给 AI 工具；
   - 禁止提交未经审计的原样 AI 输出（包括未经验证的"看起来合理"的代码）；
   - 禁止使用 AI 批量生成大量低质量、无意义的改动以刷贡献量；
   - 禁止绕过人工审计流程要求 AI"直接合入"。

维护者保留在任何时候要求贡献者解释某段代码来源的权利；无法通过审计的 AI 生成内容会被要求修改或拒绝合并。

## 提交 Pull Request

1. **Fork 并创建分支**：从 `main` 创建功能分支，建议命名如 `fix/xxx`、`feat/xxx`。
2. **保持基准同步**：提交前同步上游 `main`，避免冲突。
3. **小步提交**：一个 PR 只解决一个问题，便于审查与回滚。
4. **写清 PR 描述**：说明动机、改动内容、验证方式，并关联相关 Issue（如 `Fixes #123`）。
5. **通过 CI**：确保 GitHub Actions 中的 Build 工作流（三平台）通过后再请求合并。
6. **可评审性**：如果改动较大，可在描述中给出测试步骤，方便维护者复现。

## 代码审查

- 所有 PR 需要维护者审核通过后合入。
- 评审时关注：正确性、安全性、架构一致性、命名与可读性、是否覆盖边界情况。
- 维护者对 PR 的修改请求（`request changes`）属于正常流程，讨论应以解决问题为目标，保持友好。

## 报告问题

新建 Issue 时请尽量提供：

- 操作步骤与期望行为 / 实际行为；
- 操作系统与版本、Node.js / pnpm 版本；
- 截图或录屏（如涉及 UI 问题）。

安全相关问题（如远程代码执行、凭据泄露）请优先通过 **GitHub Security 的私人漏洞报告**（Private Vulnerability Reporting）提交，而不是公开的 Issue，以避免影响正在使用旧版本的用户。

## 行为准则

本项目希望营造一个开放、友善的社区。请：

- 使用有礼、专业的语言；
- 尊重不同的经验水平与观点；
- 对新手保持耐心，对批评保持建设性；
- 不接受人身攻击、骚扰或歧视性言论。

违反者可能被移出讨论与协作流程。

## 许可证

通过提交贡献（代码、文档、翻译等），你同意这些内容在项目的 [MIT 许可证](./LICENSE) 下发布。如有疑问，请在提交前与维护者沟通。

---

再次感谢你的贡献！如有疑问，欢迎在 Issue 中提出，或通过 [爱发电](https://afdian.com/a/Adpro) 支持本项目继续发展。